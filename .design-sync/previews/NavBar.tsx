import { NavBar } from 'bible-books-tracker'

export function Default() {
  return (
    <div style={{ background: '#0d1533', borderRadius: 10, overflow: 'hidden', minWidth: 360 }}>
      <NavBar />
    </div>
  )
}
